[workspace]
members = [
    "frutiger-config",
    "frutiger-ipc",
    "frutiger-panel",
    "frutiger-visual-tests",
]

[workspace.package]
version = "0.1.0"
description = "A scrollable-tiling Frutiger Aero Wayland compositor"
authors = ["Frutiger Rust Team"]
license = "GPL-3.0-or-later"
edition = "2021"
repository = "https://github.com/vovamasamusenkoyt/frutiger-rust"
rust-version = "1.87"

[workspace.dependencies]
anyhow = "1.0.104"
bitflags = "2.13.1"
clap = { version = "4.6.6", features = ["derive"] }
insta = "1.48.0"
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
tracing = { version = "0.1.44", features = ["max_level_trace", "release_max_level_debug"] }
tracing-subscriber = { version = "0.3.23", features = ["env-filter"] }
tracy-client = { version = "0.18.4", default-features = false }

[workspace.dependencies.smithay]
git = "https://github.com/Smithay/smithay.git"
rev = "4cf0b62028039661477d482ec4758b687d8f4392"
default-features = false

[workspace.dependencies.smithay-drm-extras]
git = "https://github.com/Smithay/smithay.git"
rev = "4cf0b62028039661477d482ec4758b687d8f4392"

[package]
name = "frutiger"
version.workspace = true
description.workspace = true
authors.workspace = true
license.workspace = true
edition.workspace = true
repository.workspace = true
rust-version.workspace = true
default-run = "frutiger"

readme = "README.md"
keywords = ["wayland", "compositor", "frutiger-aero", "liquid-glass", "wm"]

[[bin]]
name = "frutiger"
path = "src/main.rs"

[dependencies]
accesskit = { version = "0.24", optional = true }
accesskit_unix = { version = "0.22", optional = true }
anyhow.workspace = true
arrayvec = "0.7.8"
async-channel = "2.5.0"
async-io = { version = "2.6.0", optional = true }
atomic = "0.6.1"
bitflags.workspace = true
bytemuck = { version = "1.25.2", features = ["derive"] }
calloop = { version = "0.14.4", features = ["executor", "futures-io", "signals"] }
clap = { workspace = true, features = ["string"] }
clap_complete = "4.6.9"
clap_complete_nushell = "4.6.1"
directories = "6.0.0"
drm-ffi = "0.9.1"
fastrand = "2.5.0"
futures-util = { version = "0.3.33", default-features = false, features = ["std", "io"] }
git-version = "0.3.9"
glam = "0.33"
input = { version = "0.10.0", features = ["libinput_1_21"] }
keyframe = { version = "1.1.1", default-features = false }
libc = "0.2.189"
libdisplay-info = "0.3.0"
log = { version = "0.4.33", features = ["max_level_trace", "release_max_level_debug"] }
frutiger-config = { version = "0.1.0", path = "frutiger-config" }
frutiger-ipc = { version = "0.1.0", path = "frutiger-ipc", features = ["clap"] }
ordered-float = "5.3.0"
pango = { version = "0.21.5", features = ["v1_44"] }
pangocairo = "0.21.5"
pipewire = { version = "0.10.0", optional = true, features = ["v0_3_33"] }
png = "0.18.1"
portable-atomic = "1"
profiling = "1.0.18"
sd-notify = "0.5.0"
serde.workspace = true
serde_json.workspace = true
smithay-drm-extras.workspace = true
tracing-subscriber.workspace = true
tracing.workspace = true
tracy-client.workspace = true
wayland-backend = "0.3.16"
wayland-scanner = "0.31.11"
wayland-server = "0.31.14"
xcursor = "0.3.11"
zbus = { version = "5.18.0", optional = true }

[dependencies.smithay]
workspace = true
features = [
    "backend_drm",
    "backend_egl",
    "backend_gbm",
    "backend_libinput",
    "backend_session_libseat",
    "backend_udev",
    "backend_winit",
    "desktop",
    "renderer_gl",
    "renderer_pixman",
    "renderer_multi",
    "use_system_lib",
    "wayland_frontend",
]

[dev-dependencies]
approx = "0.5.1"
calloop-wayland-source = "0.4.1"
insta.workspace = true
proptest = "1.11.0"
proptest-derive = { version = "0.8.0", features = ["boxed_union"] }
rayon = "1.12.0"
wayland-client = "0.31.15"
xshell = "0.2.7"

[build-dependencies]
pkg-config = "0.3.33"

[features]
default = ["dbus", "systemd", "xdp-gnome-screencast"]
dbus = ["dep:zbus", "dep:async-io", "dep:accesskit", "dep:accesskit_unix"]
systemd = ["dbus"]
xdp-gnome-screencast = ["dbus", "pipewire"]
profile-with-tracy = ["profiling/profile-with-tracy", "tracy-client/default", "smithay/tracy_gpu_profiling"]
profile-with-tracy-ondemand = ["profile-with-tracy", "tracy-client/ondemand", "tracy-client/manual-lifetime"]
profile-with-tracy-allocations = ["profile-with-tracy"]
dinit = []

[lints.clippy]
new_without_default = "allow"
collapsible_match = "allow"

[profile.release]
debug = "line-tables-only"
overflow-checks = true
lto = "thin"

[profile.release.package.frutiger-config]
debug = false

[profile.dev.package]
insta.opt-level = 3
similar.opt-level = 3
